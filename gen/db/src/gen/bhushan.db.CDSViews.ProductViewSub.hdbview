-- generated by cds-compiler version 2.2.8
VIEW bhushan_db_CDSViews_ProductViewSub AS SELECT
  prod_0.PRODUCT_ID AS ProductId,
  texts_2.DESCRIPTION AS Description,
  (SELECT
      SUM(a_1.GROSS_AMOUNT) AS "SUM"
    FROM bhushan_db_transaction_poitems AS a_1
    WHERE a_1.PRODUCT_GUID_NODE_KEY = prod_0.NODE_KEY) AS POSum
FROM (bhushan_db_master_product AS prod_0 LEFT JOIN bhushan_db_master_product_texts AS texts_2 ON texts_2.NODE_KEY = prod_0.NODE_KEY)